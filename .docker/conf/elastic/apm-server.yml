apm-server:
  host: 0.0.0.0:8200

  kibana:
    enabled: true
    host: 'kibana:5601'
    username: elastic
    password: ${ELASTIC_PASSWORD}

  rum:
    enabled: true

output:
  elasticsearch:
    hosts: ['elasticsearch:9200']
    username: elastic
    password: ${ELASTIC_PASSWORD}
